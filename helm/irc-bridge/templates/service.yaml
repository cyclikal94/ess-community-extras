apiVersion: v1
kind: Service
metadata:
  name: {{ include "irc-bridge.fullname" . }}
  labels:
    {{- include "irc-bridge.componentLabels" (dict "context" . "component" "bridge") | nindent 4 }}
spec:
  selector:
    {{- include "irc-bridge.componentSelectorLabels" (dict "context" . "component" "bridge") | nindent 4 }}
  ports:
    - name: appservice
      protocol: TCP
      port: {{ .Values.service.appservicePort }}
      targetPort: appservice
    - name: media-proxy
      protocol: TCP
      port: {{ .Values.service.mediaProxyPort }}
      targetPort: media-proxy
