#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for matrix-appservice-irc.
#  /    |    Disable bundled services and provide explicit connection values.
#

host: irc-media.example.com

homeserver:
  url: http://ess-synapse.ess.svc.cluster.local:8008
  domain: matrix.example.com

postgres:
  enabled: false

redis:
  enabled: false
  url: redis://redis.example.com:6379/0

database:
  connectionString: postgres://matrix_irc:replace_me@postgres.example.com:5432/matrix_irc
