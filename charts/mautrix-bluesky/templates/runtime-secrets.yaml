{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-bluesky.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-bluesky.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-bluesky.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-bluesky.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-bluesky.provisioningSharedSecret" . | quote }}
{{- end }}
