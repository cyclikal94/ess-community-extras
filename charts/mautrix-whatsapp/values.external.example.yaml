#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for mautrix-whatsapp.
#  /    |    Disable bundled postgres and provide explicit connection value.
#

postgres:
  enabled: false

database:
  connectionString: postgres://mautrix_whatsapp:replace_me@postgres.example.com:5432/mautrix_whatsapp

homeserver:
  domain: matrix.example.com
