apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mautrix-whatsapp.registrationConfigMapName" . }}
  labels:
    {{- include "mautrix-whatsapp.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
data:
  {{ include "mautrix-whatsapp.registrationFileKey" . }}: |
{{ include "mautrix-whatsapp.registrationConfig" . | indent 4 }}
