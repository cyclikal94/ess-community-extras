{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-whatsapp.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-whatsapp.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-whatsapp.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-whatsapp.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-whatsapp.provisioningSharedSecret" . | quote }}
{{- end }}
