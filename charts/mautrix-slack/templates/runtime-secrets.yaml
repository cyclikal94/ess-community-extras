{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-slack.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-slack.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-slack.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-slack.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-slack.provisioningSharedSecret" . | quote }}
{{- end }}
