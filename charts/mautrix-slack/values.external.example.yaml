#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for mautrix-slack.
#  /    |    Disable bundled postgres and provide explicit connection value.
#

postgres:
  enabled: false

database:
  connectionString: postgres://mautrix_slack:replace_me@postgres.example.com:5432/mautrix_slack

homeserver:
  domain: matrix.example.com
