{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-meta.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-meta.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-meta.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-meta.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-meta.provisioningSharedSecret" . | quote }}
{{- end }}
