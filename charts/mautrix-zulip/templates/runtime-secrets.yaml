{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-zulip.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-zulip.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-zulip.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-zulip.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-zulip.provisioningSharedSecret" . | quote }}
{{- end }}
