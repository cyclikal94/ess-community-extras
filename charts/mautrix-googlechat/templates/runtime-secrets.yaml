{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-googlechat.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-googlechat.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-googlechat.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-googlechat.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-googlechat.provisioningSharedSecret" . | quote }}
{{- end }}
