{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-discord.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-discord.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-discord.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-discord.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-discord.provisioningSharedSecret" . | quote }}
{{- end }}
