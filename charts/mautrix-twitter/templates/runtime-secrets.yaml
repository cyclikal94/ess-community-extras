{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-twitter.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-twitter.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-twitter.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-twitter.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-twitter.provisioningSharedSecret" . | quote }}
{{- end }}
