{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-linkedin.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-linkedin.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-linkedin.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-linkedin.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-linkedin.provisioningSharedSecret" . | quote }}
{{- end }}
