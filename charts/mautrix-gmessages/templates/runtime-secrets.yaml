{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-gmessages.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-gmessages.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-gmessages.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-gmessages.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-gmessages.provisioningSharedSecret" . | quote }}
{{- end }}
