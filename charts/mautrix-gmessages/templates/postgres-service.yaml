{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mautrix-gmessages.postgresFullname" . }}
  labels:
    {{- include "mautrix-gmessages.componentLabels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  selector:
    {{- include "mautrix-gmessages.componentSelectorLabels" (dict "context" . "component" "postgres") | nindent 4 }}
  ports:
    - name: postgres
      protocol: TCP
      port: {{ .Values.postgres.service.port }}
      targetPort: postgres
{{- end }}
