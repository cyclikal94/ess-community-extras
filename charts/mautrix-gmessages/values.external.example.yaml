#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for mautrix-gmessages.
#  /    |    Disable bundled postgres and provide explicit connection value.
#

postgres:
  enabled: false

database:
  connectionString: postgres://mautrix_gmessages:replace_me@postgres.example.com:5432/mautrix_gmessages

homeserver:
  domain: matrix.example.com
