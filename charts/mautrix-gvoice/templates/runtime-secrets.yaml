{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-gvoice.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-gvoice.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-gvoice.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-gvoice.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-gvoice.provisioningSharedSecret" . | quote }}
{{- end }}
