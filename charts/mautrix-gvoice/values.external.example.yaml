#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for mautrix-gvoice.
#  /    |    Disable bundled postgres and provide explicit connection value.
#

postgres:
  enabled: false

database:
  connectionString: postgres://mautrix_gvoice:replace_me@postgres.example.com:5432/mautrix_gvoice

homeserver:
  domain: matrix.example.com
