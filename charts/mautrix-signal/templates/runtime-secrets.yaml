{{- if and (eq (.Values.registration.existingSecret | default "") "") .Values.registration.managedSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-signal.runtimeSecretName" . }}
  labels:
    {{- include "mautrix-signal.componentLabels" (dict "context" . "component" "registration") | nindent 4 }}
type: Opaque
stringData:
  asToken: {{ include "mautrix-signal.registrationAsToken" . | quote }}
  hsToken: {{ include "mautrix-signal.registrationHsToken" . | quote }}
  provisioningSharedSecret: {{ include "mautrix-signal.provisioningSharedSecret" . | quote }}
{{- end }}
