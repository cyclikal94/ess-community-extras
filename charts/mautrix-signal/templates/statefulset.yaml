apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mautrix-signal.fullname" . }}
  labels:
    {{- include "mautrix-signal.componentLabels" (dict "context" . "component" "bridge") | nindent 4 }}
spec:
  serviceName: {{ include "mautrix-signal.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "mautrix-signal.componentSelectorLabels" (dict "context" . "component" "bridge") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mautrix-signal.componentSelectorLabels" (dict "context" . "component" "bridge") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config-secret.yaml") . | sha256sum }}
        checksum/runtime-secrets: {{ include (print $.Template.BasePath "/runtime-secrets.yaml") . | sha256sum }}
    spec:
      {{- if .Values.podSecurityContext.enabled }}
      {{- $podSecurityContext := omit .Values.podSecurityContext "enabled" }}
      {{- with $podSecurityContext.seccompProfile }}
      {{- if ne .type "Localhost" }}
      {{- $podSecurityContext = merge (omit $podSecurityContext "seccompProfile") (dict "seccompProfile" (omit . "localhostProfile")) }}
      {{- end }}
      {{- end }}
      securityContext:
        {{- toYaml $podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "mautrix-signal.name" . }}
          image: {{ include "mautrix-signal.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - mautrix-signal
            - -c
            - /data/config.yaml
            - --no-update
          ports:
            - name: appservice
              containerPort: {{ .Values.appservice.port }}
          {{- if .Values.probes.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: appservice
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.probes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: appservice
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          {{- end }}
          {{- if .Values.securityContext.enabled }}
          securityContext:
            {{- toYaml (omit .Values.securityContext "enabled") | nindent 12 }}
          {{- end }}
          {{- with .Values.bridge.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: bridge-config
              mountPath: /data
              readOnly: true
      volumes:
        - name: bridge-config
          secret:
            secretName: {{ include "mautrix-signal.configSecretName" . }}
            items:
              - key: config.yaml
                path: config.yaml
